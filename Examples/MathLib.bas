REM ============================================
REM Test Library: MathLib.bas
REM Simple math functions for testing .bb
REM ============================================

DEF FN add$(x$, y$)
    RETURN x$ + y$
END DEF

DEF FN multiply$(x$, y$)
    RETURN x$ * y$
END DEF

DEF FN square$(x$)
    RETURN x$ * x$
END DEF
